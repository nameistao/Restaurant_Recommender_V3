import styled from "styled-components";

const StyledSlides = styled.div`
  width: 50%;
  height: 50%;
`;

const Slides = () => {
  return <StyledSlides>hey</StyledSlides>;
};

export default Slides;
