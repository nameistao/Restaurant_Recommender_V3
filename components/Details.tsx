import styled from "styled-components";

const StyledDetails = styled.div`
  width: 50%;
  height: 100%;
  color: #ffffff;
`;

const Details = () => {
  return <StyledDetails>TODO: Slides Section</StyledDetails>;
};

export default Details;
